/**
 * v0.2 defineVitestConfig / defineVitestProject — stub.
 */

export async function defineVitestConfig(_config?: unknown): Promise<never> {
  throw new Error(
    '[untestutils/config] defineVitestConfig is part of v0.2 (Nuxt in-process environment). For e2e use untestutils/vitest plugin.',
  );
}

export async function defineVitestProject(_config?: unknown): Promise<never> {
  throw new Error('[untestutils/config] defineVitestProject is part of v0.2');
}
