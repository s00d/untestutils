/**
 * Published package name so Vitest resolves `environment: 'untestutils'`.
 * Implementation lives in `@untestutils/nuxt/environment`.
 */
export { default } from '@untestutils/nuxt/environment';
